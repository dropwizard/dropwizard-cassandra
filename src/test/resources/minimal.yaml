cassandra:
  type: basic
  contactPoints: [{"host": "localhost", "port": "9142"}]
  protocolVersion:
    type: default
    version: V4
  loadBalancingPolicy:
    type: default
    localDataCenter: datacenter1
  sessionMetrics: ["continuous-cql-requests"]
  nodeMetrics: ["bytes-sent"]